local allowedPlaceIds = {
	[126509999114328] = true,
	[79546208627805]  = true,
}

local url = "https://raw.githubusercontent.com/GEC0/gec/refs/heads/main/99.Nights"

if allowedPlaceIds[game.PlaceId] then
	pcall(function()
		local remoteCode = game:HttpGet(url, true)
		local func = loadstring(remoteCode)
		if type(func) == "function" then
			func()
		end
	end)
end
